node_modules/

# Next.js build output and TypeScript build state
.next/
.next-e2e/
# The two build slots `npm run start:managed` alternates between
.next-managed-a/
.next-managed-b/
*.tsbuildinfo
# Rewritten by every dev, build, and e2e run to point at its own build directory
next-env.d.ts

# Test and coverage output
coverage/
test-results/
playwright-report/

# Local environment and provider credentials — never commit these.
.env
.env.*
!.env.example
.cert/

# Legacy runtime build output (see legacy/README.md)
!legacy/extension/out/public/index.html
out
.vscode-test/
.vsix
